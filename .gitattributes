# Auto detect text files and perform LF normalization
* text=auto

# Do not force SQLite database files into Git LFS for this deployment package.
# If maintenance_master.db is larger than GitHub allows, use Git LFS intentionally
# and confirm Streamlit Cloud has access to the real file, not a pointer.
*.db -text
